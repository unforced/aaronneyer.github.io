<%= form_for(@post) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>

	<%= f.label :title %><br />
	<%= f.text_field :title %>

	<%= f.label :content %><br />
	<%= f.text_area :content %><br />

	<%= f.submit %>
<% end %>
