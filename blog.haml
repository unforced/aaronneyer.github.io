---
layout: default
title: Blog
---

#blog
  %h2 My Blog
  == {% for post in site.posts %}
  .post.well
    %p
      %em== {{post.date | date_to_string}} >>
      %a{href: "{{post.url}}"}
        == {{post.title}}
  == {% endfor %}
